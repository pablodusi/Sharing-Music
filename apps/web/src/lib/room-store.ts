/**
 * @deprecated Phase 4 — rooms are persisted on the API.
 * Prefer `@/lib/api/client` (listPublicRooms / createRoom / joinRoom).
 * Kept only so mock-data genres remain available elsewhere.
 */
export {};
